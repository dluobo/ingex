In a new installation, create the 'ingex' user:
	./create_ingex_user.sh


Create database (see create_ingex_user.sh for password):
	./create_prodautodb.sh
	

Drop database:
	./drop_prodautodb.sh

	
Add default configuration: (see create_ingex_user.sh for password):
	./add_default_config.sh
	
	

